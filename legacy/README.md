# Preserved Claude Design exports

The original files remain in the project root as implementation references:

- `VACS Landing.dc.html`
- `VACS Landing (standalone).html`
- `export-src.html`
- `support.js`

They are not loaded by the production `index.html`.
